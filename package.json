{
  "name": "google-domains-dns-updater",
  "version": "1.0.0",
  "description": "A Node.js program for updating the dynamic DNS record of a domain hosted on Google Domains based on the public IP address of the machine it is run on.",
  "main": "index.js",
  "bin": {
    "google-domains-dns-updater": "index.js"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "axios": "^1.3.4"
  }
}